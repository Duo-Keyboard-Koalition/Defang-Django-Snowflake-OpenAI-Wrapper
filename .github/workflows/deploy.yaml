# Defang deploy disabled — use docker compose on prod machine directly
# git pull origin main && docker compose up -d --build
name: Deploy (disabled)
on:
  push:
    branches: [main]
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Defang removed. Deploy via docker compose on prod machine."
